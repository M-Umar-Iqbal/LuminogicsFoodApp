import {StyleSheet} from 'react-native';
const getStyles = () =>
  StyleSheet.create({
    title: {
      color: 'black',
    },
    container: {
      padding: 20,
    },
  });
export default getStyles;
