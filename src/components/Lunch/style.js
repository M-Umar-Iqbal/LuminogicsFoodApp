import {StyleSheet} from 'react-native';
import {colors} from '../../constants/constants';

const getStyles = () =>
  StyleSheet.create({
    title: {
      color: colors.black,
    },
    container: {
      backgroundColor: '#fff',
    },
  });
export default getStyles;
